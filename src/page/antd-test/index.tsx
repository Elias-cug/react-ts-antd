import React, { FC } from 'react';
import { Select } from 'antd';
const { Option } = Select;

const Main: FC = () => {
  return (
    <div>
      <span>单选</span>
    </div>
  );
};

export default Main;
